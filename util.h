#include <string>

using namespace std;

int StrToInteger(string s);
int HexToInteger(string h);
string DecimalToHex(int i, int w=0);
int LengthOfConstant(string s);
string BytesOfConstant(string s);